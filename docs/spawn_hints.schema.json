{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/Aedial/SuperMobTracker/blob/main/docs/spawn_hints.schema.json",
  "title": "Super Mob Tracker External Spawn Hints",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional editor schema reference; ignored by the runtime loader."
    },
    "version": {
      "type": "integer",
      "minimum": 1,
      "default": 1
    },
    "entries": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/entry"
      }
    }
  },
  "required": ["entries"],
  "$defs": {
    "resourceLocation": {
      "type": "string",
      "pattern": "^[a-z0-9_.-]+:[a-z0-9_./-]+$"
    },
    "lightLevel": {
      "type": "integer",
      "minimum": 0,
      "maximum": 15
    },
    "timeOfDayTick": {
      "type": "integer",
      "minimum": 0,
      "maximum": 23999,
      "description": "Minecraft time-of-day tick within a single day cycle. Use 0-23999."
    },
    "weather": {
      "type": "string",
      "enum": ["clear", "rain", "thunder"]
    },
    "intRange": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "min": {
          "type": "integer"
        },
        "max": {
          "type": "integer"
        }
      }
    },
    "lightRange": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "min": {
          "$ref": "#/$defs/lightLevel"
        },
        "max": {
          "$ref": "#/$defs/lightLevel"
        }
      }
    },
    "timeRange": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "start": {
          "$ref": "#/$defs/timeOfDayTick"
        },
        "end": {
          "$ref": "#/$defs/timeOfDayTick"
        }
      }
    },
    "biomes": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/resourceLocation"
          }
        },
        "types": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "BiomeDictionary.Type names such as OCEAN, SWAMP, NETHER, or END."
        },
        "allTypes": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Alternative to 'types', when ALL of the listed types must be present in the biome."
        }
      }
    },
    "summonItem": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {
          "$ref": "#/$defs/resourceLocation"
        },
        "metadata": {
          "type": "integer",
          "minimum": 0,
          "default": 0
        },
        "count": {
          "type": "integer",
          "minimum": 1,
          "default": 1
        }
      },
      "required": ["id"]
    },
    "summon": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "items": {
          "type": "array",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/summonItem"
          }
        },
        "onBlock": {
          "$ref": "#/$defs/resourceLocation"
        },
        "onEntity": {
          "$ref": "#/$defs/resourceLocation"
        }
      },
      "required": ["items"]
    },
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "entityId": {
          "$ref": "#/$defs/resourceLocation"
        },
        "parent": {
          "$ref": "#/$defs/resourceLocation",
          "description": "Optional. Use the conditions of an existing parent entry instead of repeating them. Must be a declared entityId in the file."
        },
        "spawnReason": {
          "type": "string",
          "description": "Reason shown in the GUI. Known values such as worldgen, structure, spawner, event, command, and other have built-in labels."
        },
        "biomes": {
          "$ref": "#/$defs/biomes"
        },
        "dimensionId": {
          "type": "integer",
          "description": "Optional. If omitted, Super Mob Tracker tries to infer it from the resolved biome list and otherwise falls back to the current dimension."
        },
        "dimensionName": {
          "type": "string"
        },
        "groundBlocks": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "lightLevels": {
          "$ref": "#/$defs/lightRange",
          "description": "Light levels use the vanilla Minecraft range 0-15."
        },
        "yLevels": {
          "$ref": "#/$defs/intRange"
        },
        "timeOfDay": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/timeRange"
          },
          "description": "Time-of-day ranges in ticks, using the 0-23999 day cycle."
        },
        "weather": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/weather"
          },
          "uniqueItems": true
        },
        "requiresSky": {
          "type": "boolean"
        },
        "moonPhases": {
          "type": "array",
          "items": {
            "type": "integer",
            "minimum": 0,
            "maximum": 7
          }
        },
        "requiresSlimeChunk": {
          "type": "boolean"
        },
        "requiresNether": {
          "type": "boolean"
        },
        "summon": {
          "$ref": "#/$defs/summon",
          "description": "Items used to summon the entity. The GUI resolves each item dynamically for its localized tooltip and JEI navigation. onBlock and onEntity are optional targets; if both are given, onBlock takes precedence."
        },
        "hints": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["entityId"],
      "anyOf": [
        {"required": ["spawnReason"]},
        {"required": ["parent"]}
      ]
    }
  }
}